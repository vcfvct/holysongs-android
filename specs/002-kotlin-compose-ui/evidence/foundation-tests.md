# T004/T005 — Host characterization before replacement

**Date:** 2026-09-15, America/New_York. **Result:** passed, host scope only.
**Source:** f62519b4bff420ab3f78b1d544732c25683f4772 plus the test changes in this working tree.
No new APK or device results are claimed. Runtime API/WebView fields are not applicable.

## Independent expectations

`app/src/test/resources/catalog-baseline.xml` is an exact frozen copy of
`git show f62519b4bff420ab3f78b1d544732c25683f4772:assets/songs.xml`, extracted before tests.
Its SHA-256 is `88eb0db602e018b49a327947dd8607f04e6159e58f39ec38ed59f20c39af9d89`.
Expected raw values are read from this test-only committed-input snapshot with JAXP textContent,
not generated by XMLParser or from the working asset at test runtime. Actual values use the real
production XMLParser. No sorting/loading implementation is copied into the tests.

SourceSong meanings from data-model.md:

- "Zero-based XML position; all 422 entries stay in original order"
- "Raw parsed name, before production space-removal preprocessing"
- "Raw parsed lyric, including line breaks and original text"

All source names and full raw lyrics are compared position-by-position. Independently fixed pairs:
轻轻听42/320, 以色列的圣者53/373, 耶稣基督是主87/123, 耶稣耶稣102/110,
天堂在我心109/351, 全地宣告143/144, 像天空的鸽子213/214, 愿您崇高341/342.
Representative first/winning lyrics also have exact literal whitespace/newline/Chinese assertions.
An explicit nested-text fixture characterizes first-TEXT_NODE extraction rather than silently
switching to trim/textContent in production. Source asset and JAR hashes remain asserted.

Polyphonic first-reading literals were reviewed independently from the pinned JAR's
`pinyindb/unicode_to_hanyu_pinyin.txt`: 重=zhong4/chong2, 行=xing2/hang2/hang4/xing4/heng2,
长=zhang3/chang2, 乐=le4/yue4. Tests preserve first reading, lowercase/no-tone/Unicode-ü output,
and non-Chinese fallback including spacing, newlines and supplementary characters.
Comparator coverage exercises reflexivity, antisymmetry, transitivity, equality consistency and
China-locale delegation over Chinese/polyphonic/duplicate/non-Chinese inputs. **Host Collator
results are not Android ordering fixtures.** Helpers and sorting/pinyin algorithms are unchanged.

## Execution

1. `./gradlew :app:testDebugUnitTest --console=plain` — succeeded; initial run reused production
   compile outputs. Followed with an explicit uncached full task rerun rather than relying on that.
2. `./gradlew :app:testDebugUnitTest --rerun-tasks --no-build-cache --console=plain` — succeeded,
   23 actionable tasks executed, including production javac and test javac/JUnit.

| Suite | Tests | Failures | Errors | Skipped |
|---|---:|---:|---:|---:|
| BuildInputsTest | 2 | 0 | 0 | 0 |
| SongCatalogTest | 4 | 0 | 0 | 0 |
| ChineseCharCompTest | 2 | 0 | 0 | 0 |
| HanziHelperTest | 4 | 0 | 0 | 0 |
| Total | 12 | 0 | 0 | 0 |

Toolchain: Gradle9.6.0/AGP9.4.0, Homebrew JDK17 launcher, owner-approved Studio JBR25 daemon,
existing Java17 compilation toolchain/Java8 app targets, existing SDK37/Build Tools36.0.0.
Raw logs are private at `/tmp/holysongs-002-foundation-unit-tests.log` and
`/tmp/holysongs-002-foundation-unit-tests-fresh.log`; regenerated reports are under
`app/build/test-results/testDebugUnitTest/` (not tracked).

Before execution, the previously observed trailing `1` owner edit disappeared from MainActivity.java;
`git diff` for that file is now empty. The agent did not remove it. Thus no syntax failure was
observed by javac and no invented failing regression is claimed. These are characterization tests
passing against the existing implementation, not proof of fixed runtime behavior. Existing Kotlin
opt-out/deprecation warnings remain visible; no broad suppression added.

T004/T005 completed from these results. Compose build, native order/letter/winner/payload fixtures,
API23/API37 baseline capture and all later runtime acceptance are still separate gates.
